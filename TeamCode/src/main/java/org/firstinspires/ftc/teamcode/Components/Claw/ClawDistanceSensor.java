package org.firstinspires.ftc.teamcode.Components.Claw;

public class ClawDistanceSensor {
    public ClawDistanceSensor() {

    }

    //look at and return distance to the nearest cone
    public double observeConeDistance() {
        //no input
        //no state conditions
        //execute algorithm for observing
        //no setting state
        //log to general robot log that the cone has been observed through function closeClaw()



        //just placeholder so that there are no errors
        return 2.0;
    }

    //look at and return distance to the top of the junction
    public double observeJunctionDistance() {
        //no input
        //no state conditions
        //execute algorithm for observing
        //no setting state
        //log to general robot log that the junction has been observed through function closeClaw()



        //just placeholder so that there are no errors
        return 2.0;
    }
}
