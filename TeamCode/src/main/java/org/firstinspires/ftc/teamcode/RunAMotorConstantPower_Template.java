package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.Disabled;
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;
import com.qualcomm.robotcore.hardware.DcMotor;
import com.qualcomm.robotcore.hardware.DcMotorSimple;

/**
 * Assignment:
 * Set the motor to run at 70% power forwards when the B buttons is pushed on
 * the gamepad. Using telemetry, show the value of the B button as well as the speed
 * of the motor
 *
 * Resources that might be helpful:
 * RunAMotorConstantPower.java
 * Gamepad class definition
 */

@TeleOp
@Disabled
public class RunAMotorConstantPower_Template extends LinearOpMode{

    @Override
    public void runOpMode() throws InterruptedException {

        // runs until the end of the match (until the driver presses STOP
        // Test
        while(opModeIsActive()){

        }
    }
}
