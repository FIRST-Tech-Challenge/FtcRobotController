package org.firstinspires.ftc.teamcode.util;

/** This interface currently means only one thing; the implementors can be configured (generally using configMan) but not necessarily. */
public interface Configurable {
}
